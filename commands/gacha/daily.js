module.exports = {
    name: 'daily',
    category: 'gacha',
    devOnly: false,
    permissions: [],
    run: async ({bot, message, args}) => {
        daily(message);
    }
};

function daily(message){

}